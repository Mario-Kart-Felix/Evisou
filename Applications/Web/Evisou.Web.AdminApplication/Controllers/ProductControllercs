using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using Evisou.Web.AdminApplication.Common;
using Evisou.Ims.Contract.Model;

namespace Evisou.Web.AdminApplication.Controllers
{
    //[RoutePrefix("ims/api/product")]
    public class ProductController : ApiController//AdminApiControllerBase
    {
        public IHttpActionResult GetAllProducts()
        {
          //  var allProduct = this.ImsService.GetProductList(null);

           // IEnumerable<Product> filterProduct = allProduct;

            return Ok("dsadas");
        }
    }
}
